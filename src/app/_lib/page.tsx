//this whole file with underscore in prefix of folder name is private and not considered by routing system

export default function PrivateFolder(){
    return(
        <div>
            <h1>Private Folder and cannot be viewed in browser</h1>
        </div>
    )
}